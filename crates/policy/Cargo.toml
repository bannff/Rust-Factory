[package]
name = "policy"
description = "Transport-independent trusted context and capability grant policy contracts."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[package.metadata.rust-factory]
family = "policy"
role = "brick"
status = "implemented"

# No feature is enabled by default. This brick has no `mcp` feature on purpose:
# the authorization substrate is not agent-drivable.
[features]
memory = []

[dependencies]
sha2.workspace = true

[lints]
workspace = true
