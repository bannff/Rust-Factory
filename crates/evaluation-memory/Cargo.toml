[package]
name = "evaluation-memory"
description = "Deterministic in-memory adapters for Rust Factory evaluation."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[package.metadata.rust-factory]
family = "evaluation"
role = "memory"
status = "implemented"

[dependencies]
evaluation = { path = "../evaluation", version = "=0.1.0" }
workflow = { path = "../workflow", version = "=0.1.0" }

[lints]
workspace = true
