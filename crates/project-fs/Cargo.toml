[package]
name = "project-fs"
description = "Root-confined filesystem adapter for Rust projects."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[package.metadata.rust-factory]
family = "project"
role = "adapter"
status = "implemented"

[dependencies]
cap-std.workspace = true
project-core = { path = "../project-core", version = "=0.1.0" }

[lints]
workspace = true
