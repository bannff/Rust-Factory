[package]
name = "agent-mcp"
description = "MCP adapter for bounded Rust Factory agent definitions."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[package.metadata.rust-factory]
family = "agent"
role = "mcp"
status = "migration-pending"

[dependencies]
agent = { path = "../agent", version = "=0.1.0" }
anyhow.workspace = true
mcp-transport = { path = "../mcp-transport", version = "=0.1.0" }
policy = { path = "../policy", version = "=0.1.0" }
rmcp.workspace = true
schemars.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["io-util"] }

[lints]
workspace = true
