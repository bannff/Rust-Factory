# Tasks

- [x] Add ceiling model, validation, canonical SHA-256 digest contribution, and intersection in `agent-core`.
- [x] Add `invoke_with_ceiling` and preserve current `invoke` as full-definition compatibility wrapper.
- [x] Add denial-before-model/adapter, no-elevation, single-definition-snapshot, and digest tests.
- [x] Run QA/security/Rust-SME/architecture acceptance and `make check` (all approved/passed).
- [x] Policy-to-Agent MCP migration is deferred until `policy-core` is accepted.