# Tasks

- [x] Obtain formal Rust SME approval for the canonical taxonomy, package roles, ingress/egress sequence, `serde`/`schemars` validation layering, and MCP lifecycle correction.
- [x] Add an approved architecture reference to the Living Factory Vision, README, and scaffolding/project-blueprint guidance; preserve the portfolio tracker as the delivery-status source of truth. **Current interpretation:** GitHub issues own family/taxonomy decisions and GitHub Projects owns delivery tracking; the Vision is architectural guidance, not a registry.
- [x] Revise the canonical standard and Living Factory Vision for mandatory agent-maintainable status-only core scaffolds, the `package.metadata.rust-factory` family/role/status registry, fixed placeholder paths, role eligibility, and the family-level mature-shape taxonomy. **Superseded terminology:** metadata remains the authoritative record for an existing package, but it is not a family roadmap registry; the closed roles are now `brick`, `core`, `infrastructure`, `server`, `mesh`, and `test-support`.
- [x] Deliver the deterministic metadata/layout validator in the first status-only rollout; it rejects unknown roles/statuses, missing required status-only paths, prohibited dependencies, canonical scaffold metadata/path mismatches, and Vision/package scaffold-status drift. **Historical scope, since superseded:** the current validator enforces local workspace/package structure, metadata, status-only shape, adapter isolation, and Makefile coverage. It does not compare package state with the Vision or any external roadmap.
- [x] Revise the taxonomy so a registry row rather than an empty package is the parking space for an undriven capability family (#11): prune the 9 families with no consumer, fold tool execution into Sandbox, generalize the validator to every workspace package with bidirectional registry agreement, add the `adapter` and `infrastructure` roles, and require libraries under `crates/` with binaries under `projects/`. **Historical decision, since superseded:** an undriven family is roadmap-only in its GitHub issue and GitHub Projects entry, not a local or Vision registry row. The validator does not enforce external roadmap agreement, `adapter` is no longer a package role, and adapters are feature-gated modules in a `role = "brick"` crate.
- [ ] Extend metadata/layout validation with locally decidable semantic contract checks after each family has an independently approved contract, including package-role eligibility and adapter-module layout rules. Do not treat external GitHub roadmap membership as a validator concern.
- [ ] ~~Add a role-based dependency-direction check in which `role = "core"` packages reject dependencies on retired per-adapter package roles.~~ **Superseded; do not implement as written:** `core` is now reserved for status-only packages, mature capability crates use `role = "brick"`, and `mcp`, `adapter`, `vendor`, and `memory` are modules rather than package roles. Any future dependency-direction check must model the current six-role vocabulary and the module-level adapter boundary without claiming to validate the external roadmap.
- [ ] Add `projects/` with the first composition-root binary so the workspace produces a runnable artifact. The `serve_stdio` conflict is resolved: the helper is deleted and the binary binds the transport. One question remains, whether a multi-brick composition root declares an existing family or a new non-capability `composition` family. The validator already exempts `role = "server"` from the family-prefix naming rule so a neutrally named deployable is possible.
- [ ] Create one focused specification and delivery-gated semantic implementation at a time. Cache, graph, and message-bus are candidates, not a combined batch; start only after each family demonstrates a consumer, stable core-owned port, and independently approved contract.
- [ ] Design a dedicated versioned Project Blueprint successor for canonical family stamping. Project Blueprint V1 remains unchanged and SHALL NOT stamp package families.

## Acceptance evidence

- Formal Rust SME approval resolves the current lifecycle/taxonomy blockers.
- Architecture confirms the standard preserves inward dependencies, thin composition roots, and the Living Factory Vision.
- The standard names no unapproved framework dependency; `serde` and `schemars` remain boundary-only.
- Follow-on lifecycle migrations retain MCP schemas, bounded transport behavior, trusted-context/Policy semantics, and public error projections under their own tests and delivery gates.
